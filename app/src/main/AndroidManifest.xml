<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qrcode">

    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".app.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".app.started.StartedActivity"/>
        <activity android:name=".app.pin.PinActivity"/>
        <activity android:name=".app.register.RegisterActivity"/>
        <activity android:name=".app.home.HomeActivity"/>
        <activity android:name=".app.showqr.ShowQrDialog"
            android:theme="@style/Theme.Base.Dialog"
            />
        <activity android:name=".app.scan.ScanActivity"/>
        <activity android:name=".app.transfer.TransferActivity"/>
        <activity android:name=".app.topup.TopUpActivity"
            android:theme="@style/Theme.Base.Dialog"/>
    </application>

</manifest>