<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="com.example.qrcode.model.User"/>
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="com.example.user.cecilia.SignUpActivity"
        android:background="@drawable/background">

        <TextView
            android:id="@+id/tvTitleSignUp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Sign Up"
            android:textColor="@color/colorWhite"
            android:textSize="50sp"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toTopOf="@+id/llBoxSignUp" />

        <LinearLayout
            android:id="@+id/llBoxSignUp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/tvTitleSignUp"
            app:layout_constraintBottom_toBottomOf="parent"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Username"
                android:textColor="@color/colorWhite" />

            <EditText
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ems="10"
                android:inputType="textEmailAddress"
                android:text="@={viewModel.email}"
                android:drawableLeft="@drawable/ic_email"
                android:backgroundTint="@color/colorWhite"
                android:background="@drawable/border_edittext"
                android:padding="@dimen/common_12dp"
                android:textCursorDrawable="@null"
                android:textColor="@color/colorWhite"
                android:drawablePadding="10dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Password"
                android:textColor="@color/colorWhite"
                android:layout_marginTop="10dp" />

            <EditText
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ems="10"
                android:inputType="textPassword"
                android:text="@={viewModel.password}"
                android:drawableLeft="@drawable/ic_lock"
                android:backgroundTint="@color/colorWhite"
                android:background="@drawable/border_edittext"
                android:padding="@dimen/common_12dp"
                android:textCursorDrawable="@null"
                android:textColor="@color/colorWhite"
                android:drawablePadding="10dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Name"
                android:textColor="@color/colorWhite"
                android:layout_marginTop="10dp" />

            <EditText
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ems="10"
                android:inputType="text"
                android:text="@={viewModel.name}"
                android:drawableLeft="@drawable/ic_person"
                android:backgroundTint="@color/colorWhite"
                android:background="@drawable/border_edittext"
                android:padding="@dimen/common_12dp"
                android:textCursorDrawable="@null"
                android:textColor="@color/colorWhite"
                android:drawablePadding="10dp" />

            <Button
                android:id="@+id/btnSignUp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="SIGN UP"
                android:textColor="@color/colorWhite"
                android:background="@android:color/transparent"
                android:layout_gravity="center"
                android:layout_marginTop="30dp" />

        </LinearLayout>

    </android.support.constraint.ConstraintLayout>
</layout>